import { registerRootComponent } from 'expo';
import App from './App';

// Entry point. registerRootComponent wires App to AppRegistry for both
// the dev client and a standalone release build.
registerRootComponent(App);
